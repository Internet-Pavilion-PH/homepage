// Disable server-side rendering for the entire site from this layout
export const ssr = false;

// Note: keeping prerender default. If you want to disable prerendering too,
// uncomment the next line.
// export const prerender = false;
